# Architecture

Control Plane-provide UI to perform -like aws databricks
Compute Plane-take care compute power
    a. serverless--will be managed by databricks
    b. classic compute- will be managed by cloud provider